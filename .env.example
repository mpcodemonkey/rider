# ---------------------------------------------------------------------------
# Required
# ---------------------------------------------------------------------------
# Bot token from your Fluxer instance's developer portal.
FLUXER_TOKEN=

# Base API URL. Leave unset for fluxer.app; point it at your own instance
# when self-hosting, e.g. https://fluxer.example.com/api/v1
#FLUXER_API_URL=

# ---------------------------------------------------------------------------
# Commands
# ---------------------------------------------------------------------------
# Jockie's default. Matching is case-insensitive, so M! works too.
BOT_PREFIX=m!
# Extra prefixes, comma separated. Handy when running several instances
# (Jockie style: mm! for the second bot, mmm! for the third).
#BOT_EXTRA_PREFIXES=!,-
BOT_RESPOND_TO_MENTION=true

# ---------------------------------------------------------------------------
# Playback
# ---------------------------------------------------------------------------
DEFAULT_VOLUME=100
MAX_VOLUME=200
MAX_QUEUE_SIZE=5000
# Seconds to wait before leaving an idle channel. 0 disables it.
IDLE_TIMEOUT=300
# Seconds to wait before leaving a channel with no listeners. 0 disables it.
EMPTY_CHANNEL_TIMEOUT=60
ANNOUNCE_NOW_PLAYING=true

# ---------------------------------------------------------------------------
# Sources
# ---------------------------------------------------------------------------
# ytsearch (YouTube) or scsearch (SoundCloud).
SEARCH_PROVIDER=ytsearch
YTDLP_FORMAT=bestaudio/best
# Max tracks pulled from one playlist/album.
PLAYLIST_LIMIT=500
# Netscape-format cookie file. Helps with age-gated videos and bot checks.
# Only takes effect for a client that honors cookies (see YTDLP_PLAYER_CLIENTS
# below) - export with a browser extension like "Get cookies.txt LOCALLY",
# logged into a real Google account, and refresh it if it stops working.
#YTDLP_COOKIEFILE=/config/cookies.txt
#YTDLP_PROXY=http://proxy:8080
# Which YouTube client(s) yt-dlp pretends to be, tried in order, comma
# separated. Two separate things can go wrong here, with different symptoms:
#   - no cookies sent          -> "Sign in to confirm you're not a bot"
#   - no PO token (none configured by this bot) -> "Requested format is
#     not available"
# 'tv' is the default because it's the only client that needs neither:
# it sends cookies and doesn't require a PO token on any protocol. 'web'
# also sends cookies but does require a PO token for most formats, so it's
# a secondary attempt, not the lead. 'android'/'mweb'/'ios' need a PO token
# AND ignore cookies entirely - don't lead with them.
# If YouTube changes what works, check the yt-dlp README/issues for the
# current guidance and adjust this - no code change needed.
YTDLP_PLAYER_CLIENTS=tv,web

# URL of a bgutil-ytdlp-pot-provider server (see the "PO token provider"
# section in README.md). Only needed if YouTube keeps blocking you even with
# YTDLP_COOKIEFILE correctly set — common when the bot runs on a datacenter/
# VPS IP that gets flagged regardless of valid cookies. Leave unset until you
# actually hit that; 'tv' above already avoids the PO token requirement for
# most videos on its own.
#YTDLP_POT_PROVIDER_URL=http://bgutil-pot-provider:4416

# Spotify links are resolved to metadata and then played from YouTube.
# Create an app at https://developer.spotify.com/dashboard
#SPOTIFY_CLIENT_ID=
#SPOTIFY_CLIENT_SECRET=

# Allow playing files from the bot's own filesystem. Only enable this if you
# trust everyone who can run commands — it exposes the bot's local disk.
ALLOW_LOCAL_FILES=false

# ---------------------------------------------------------------------------
# Infrastructure
# ---------------------------------------------------------------------------
FFMPEG_PATH=ffmpeg
DATABASE_PATH=data/jockiefluxer.db
LOG_LEVEL=INFO
